"use strict";
// normal function
function addNew(first, scound) {
    const result = first + scound;
    return result;
}
const outPut1 = add(45, 25);
// arrow function
const doMath = (num1, num2) => num1 + num2;
const mathResult = (x, y) => {
    return x + y;
};
