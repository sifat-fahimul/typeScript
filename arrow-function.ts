// normal function
function addNew(first: number, scound: number): number {
    const result = first + scound;
    return result;
}
const outPut1: number = add(45, 25)


// arrow function

const doMath = (num1: number, num2: number): number => num1 + num2;

const mathResult = (x: number, y: number): number => {
    return x + y;
}